{
  "dependencies": {
    "@jest/globals": "^29.7.0",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.7",
    "eslint": "^9.21.0",
    "express": "^4.21.2",
    "supertest": "^7.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.13.9",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "nodemon": "^3.1.9",
    "ts-jest": "^29.2.6",
    "tsx": "^4.19.3"
  },
  "name": "static-content-server",
  "version": "1.0.0",
  "description": "An Express-based server to serve static content.",
  "main": "dist/app.js",
  "scripts": {
    "build": "npx tsc",
    "start": "node .",
    "dev": "nodemon -x 'tsx' -w src -w package.json -w tsconfig.json -w .env src/app.ts",
    "test": "cross-env NODE_ENV=test NODE_OPTIONS='$NODE_OPTIONS --experimental-vm-modules' jest --testTimeout=5000",
    "lint": "eslint"
  },
  "repository": {
    "type": "git",
    "url": "github.com/blastoffwaters/static-content-server"
  },
  "keywords": [
    "image-server",
    "static-server",
    "express",
    "server"
  ],
  "author": "Blast Off Waters",
  "license": "AGPL-3.0"
}
